services:
  - type: web
    name: campus-canvas
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: CEMS_SECRET
        generateValue: true
